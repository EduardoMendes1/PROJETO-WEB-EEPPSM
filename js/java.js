ScrollReveal({
    // reset: true,
    distance: '80px',
    duration: 2000,
    delay: 200
});
ScrollReveal().reveal('.right-side, .left-side', { origin: 'left' });
ScrollReveal().reveal('.right-side, .left-side', { origin: 'right' });